# Redis Configuration (Optional - Railway provides managed Redis)
# For local development, use: REDIS_HOST=localhost
REDIS_HOST=your-redis-host
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password

# Railway will automatically set PORT - don't change this
# PORT=8000