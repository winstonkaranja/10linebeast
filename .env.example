# Redis Configuration
# 
# RAILWAY DEPLOYMENT SETUP:
# 1. In Railway dashboard, add a Redis database service to your project
# 2. In your main service, add environment variable:
#    - Name: REDIS_URL
#    - Value: ${{ Redis.REDIS_URL }}
# 3. Deploy - Redis will be automatically connected
#
# For Railway: (set via dashboard, not in code)
# REDIS_URL=${{ Redis.REDIS_URL }}

# LOCAL DEVELOPMENT:
# Start local Redis server: redis-server
# Then uncomment and configure:
# REDIS_URL=redis://localhost:6379
# OR use individual settings:
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=your-redis-password

# Railway will automatically set PORT - don't change this
# PORT=8000