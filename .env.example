# Redis Configuration
# Railway deployment (preferred): Use REDIS_URL variable from Railway Redis service
# Set this in Railway variables: REDIS_URL=${{ Redis.REDIS_URL }}
REDIS_URL=redis://localhost:6379

# Alternative: Individual Redis settings (for local development)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=your-redis-password

# Railway will automatically set PORT - don't change this
# PORT=8000